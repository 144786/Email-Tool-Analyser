# HINT: in the DS solution, the following are used.
from os import walk
from os import path # before you create a direcory, use this the check if it's already there
from os import mkdir
import mailparser

def get_attachements(email_folder : str):
    """
    Get all of the emails in the folder locate at the path provided, get the emails, get their attachments, 
    and store the attachments in a folder called 'found attachments

    Args:
        email_folder (str): 
        The path to the folder that contains all the emails.
    """
# TODO
pass

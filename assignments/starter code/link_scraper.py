# HINT: The DS solution contains following:
from urlextract import URLExtract
import mailparser
from os import walk


def scrape_links(emails_folder : str):
    """
    Take in a string containing a directory of emails. For each email, find all the links in the body, then write all
    the links found into a text file.

    Args:
        emails_folder (str):
        A string containing a directory of emails.
    """
    # TODO
    pass
